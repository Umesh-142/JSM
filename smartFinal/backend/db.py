# from flask_pymongo import PyMongo
# from flask import Flask

# app = Flask(__name__)

# # Connect to MongoDB (Change 'mongodb://localhost:27017/ai_task_db' if using a cloud database)
# app.config["MONGO_URI"] = "mongodb://localhost:27017/ai_task_db"
# mongo = PyMongo(app)
